import React from 'react';

interface WorkExpProps {
  id?: string;
}

const WorkExp = ({ id }: WorkExpProps) => {
    return (
        <div id={id}>
            
        </div>
    );
};

export default WorkExp;